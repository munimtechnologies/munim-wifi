///
/// JSuggestionOutcome.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#include <fbjni/fbjni.h>
#include "SuggestionOutcome.hpp"

#include "JSuggestionStatus.hpp"
#include "SuggestionStatus.hpp"
#include <optional>
#include <string>

namespace margelo::nitro::munimwifi {

  using namespace facebook;

  /**
   * The C++ JNI bridge between the C++ struct "SuggestionOutcome" and the Kotlin data class "SuggestionOutcome".
   */
  struct JSuggestionOutcome final: public jni::JavaClass<JSuggestionOutcome> {
  public:
    static constexpr auto kJavaDescriptor = "Lcom/margelo/nitro/munimwifi/SuggestionOutcome;";

  public:
    /**
     * Convert this Java/Kotlin-based struct to the C++ struct SuggestionOutcome by copying all values to C++.
     */
    [[maybe_unused]]
    [[nodiscard]]
    SuggestionOutcome toCpp() const {
      static const auto clazz = javaClassStatic();
      static const auto fieldStatus = clazz->getField<JSuggestionStatus>("status");
      jni::local_ref<JSuggestionStatus> status = this->getFieldValue(fieldStatus);
      static const auto fieldSuggestionId = clazz->getField<jni::JString>("suggestionId");
      jni::local_ref<jni::JString> suggestionId = this->getFieldValue(fieldSuggestionId);
      static const auto fieldMessage = clazz->getField<jni::JString>("message");
      jni::local_ref<jni::JString> message = this->getFieldValue(fieldMessage);
      return SuggestionOutcome(
        status->toCpp(),
        suggestionId != nullptr ? std::make_optional(suggestionId->toStdString()) : std::nullopt,
        message != nullptr ? std::make_optional(message->toStdString()) : std::nullopt
      );
    }

  public:
    /**
     * Create a Java/Kotlin-based struct by copying all values from the given C++ struct to Java.
     */
    [[maybe_unused]]
    static jni::local_ref<JSuggestionOutcome::javaobject> fromCpp(const SuggestionOutcome& value) {
      using JSignature = JSuggestionOutcome(jni::alias_ref<JSuggestionStatus>, jni::alias_ref<jni::JString>, jni::alias_ref<jni::JString>);
      static const auto clazz = javaClassStatic();
      static const auto create = clazz->getStaticMethod<JSignature>("fromCpp");
      return create(
        clazz,
        JSuggestionStatus::fromCpp(value.status),
        value.suggestionId.has_value() ? jni::make_jstring(value.suggestionId.value()) : nullptr,
        value.message.has_value() ? jni::make_jstring(value.message.value()) : nullptr
      );
    }
  };

} // namespace margelo::nitro::munimwifi
