///
/// SuggestionOutcome.swift
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

import NitroModules
import CxxStdlib

/**
 * Represents an instance of `SuggestionOutcome`, backed by a C++ struct.
 */
public typealias SuggestionOutcome = margelo.nitro.munimwifi.SuggestionOutcome

public extension SuggestionOutcome {
  private typealias bridge = margelo.nitro.munimwifi.bridge.swift

  /**
   * Create a new instance of `SuggestionOutcome`.
   */
  init(status: SuggestionStatus, suggestionId: String?, message: String?) {
    self.init(status, { () -> bridge.std__optional_std__string_ in
      if let __unwrappedValue = suggestionId {
        return bridge.create_std__optional_std__string_(std.string(__unwrappedValue))
      } else {
        return .init()
      }
    }(), { () -> bridge.std__optional_std__string_ in
      if let __unwrappedValue = message {
        return bridge.create_std__optional_std__string_(std.string(__unwrappedValue))
      } else {
        return .init()
      }
    }())
  }

  @inline(__always)
  var status: SuggestionStatus {
    return self.__status
  }
  
  @inline(__always)
  var suggestionId: String? {
    return { () -> String? in
      if bridge.has_value_std__optional_std__string_(self.__suggestionId) {
        let __unwrapped = bridge.get_std__optional_std__string_(self.__suggestionId)
        return String(__unwrapped)
      } else {
        return nil
      }
    }()
  }
  
  @inline(__always)
  var message: String? {
    return { () -> String? in
      if bridge.has_value_std__optional_std__string_(self.__message) {
        let __unwrapped = bridge.get_std__optional_std__string_(self.__message)
        return String(__unwrapped)
      } else {
        return nil
      }
    }()
  }
}
