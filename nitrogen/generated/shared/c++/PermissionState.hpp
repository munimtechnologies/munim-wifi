///
/// PermissionState.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::munimwifi {

  /**
   * An enum which can be represented as a JavaScript union (PermissionState).
   */
  enum class PermissionState {
    RESTRICTED      SWIFT_NAME(restricted) = 0,
    GRANTED      SWIFT_NAME(granted) = 1,
    DENIED      SWIFT_NAME(denied) = 2,
    NOTDETERMINED      SWIFT_NAME(notdetermined) = 3,
    UNAVAILABLE      SWIFT_NAME(unavailable) = 4,
  } CLOSED_ENUM;

} // namespace margelo::nitro::munimwifi

namespace margelo::nitro {

  // C++ PermissionState <> JS PermissionState (union)
  template <>
  struct JSIConverter<margelo::nitro::munimwifi::PermissionState> final {
    static inline margelo::nitro::munimwifi::PermissionState fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("restricted"): return margelo::nitro::munimwifi::PermissionState::RESTRICTED;
        case hashString("granted"): return margelo::nitro::munimwifi::PermissionState::GRANTED;
        case hashString("denied"): return margelo::nitro::munimwifi::PermissionState::DENIED;
        case hashString("notDetermined"): return margelo::nitro::munimwifi::PermissionState::NOTDETERMINED;
        case hashString("unavailable"): return margelo::nitro::munimwifi::PermissionState::UNAVAILABLE;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum PermissionState - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::munimwifi::PermissionState arg) {
      switch (arg) {
        case margelo::nitro::munimwifi::PermissionState::RESTRICTED: return JSIConverter<std::string>::toJSI(runtime, "restricted");
        case margelo::nitro::munimwifi::PermissionState::GRANTED: return JSIConverter<std::string>::toJSI(runtime, "granted");
        case margelo::nitro::munimwifi::PermissionState::DENIED: return JSIConverter<std::string>::toJSI(runtime, "denied");
        case margelo::nitro::munimwifi::PermissionState::NOTDETERMINED: return JSIConverter<std::string>::toJSI(runtime, "notDetermined");
        case margelo::nitro::munimwifi::PermissionState::UNAVAILABLE: return JSIConverter<std::string>::toJSI(runtime, "unavailable");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert PermissionState to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("restricted"):
        case hashString("granted"):
        case hashString("denied"):
        case hashString("notDetermined"):
        case hashString("unavailable"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
