///
/// SuggestionStatus.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::munimwifi {

  /**
   * An enum which can be represented as a JavaScript union (SuggestionStatus).
   */
  enum class SuggestionStatus {
    UNKNOWN      SWIFT_NAME(unknown) = 0,
    UNSUPPORTED      SWIFT_NAME(unsupported) = 1,
    FAILED      SWIFT_NAME(failed) = 2,
    ADDED      SWIFT_NAME(added) = 3,
    ALREADYEXISTS      SWIFT_NAME(alreadyexists) = 4,
    REMOVED      SWIFT_NAME(removed) = 5,
    NOTFOUND      SWIFT_NAME(notfound) = 6,
    ACTIVE      SWIFT_NAME(active) = 7,
    INACTIVE      SWIFT_NAME(inactive) = 8,
  } CLOSED_ENUM;

} // namespace margelo::nitro::munimwifi

namespace margelo::nitro {

  // C++ SuggestionStatus <> JS SuggestionStatus (union)
  template <>
  struct JSIConverter<margelo::nitro::munimwifi::SuggestionStatus> final {
    static inline margelo::nitro::munimwifi::SuggestionStatus fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("unknown"): return margelo::nitro::munimwifi::SuggestionStatus::UNKNOWN;
        case hashString("unsupported"): return margelo::nitro::munimwifi::SuggestionStatus::UNSUPPORTED;
        case hashString("failed"): return margelo::nitro::munimwifi::SuggestionStatus::FAILED;
        case hashString("added"): return margelo::nitro::munimwifi::SuggestionStatus::ADDED;
        case hashString("alreadyExists"): return margelo::nitro::munimwifi::SuggestionStatus::ALREADYEXISTS;
        case hashString("removed"): return margelo::nitro::munimwifi::SuggestionStatus::REMOVED;
        case hashString("notFound"): return margelo::nitro::munimwifi::SuggestionStatus::NOTFOUND;
        case hashString("active"): return margelo::nitro::munimwifi::SuggestionStatus::ACTIVE;
        case hashString("inactive"): return margelo::nitro::munimwifi::SuggestionStatus::INACTIVE;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum SuggestionStatus - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::munimwifi::SuggestionStatus arg) {
      switch (arg) {
        case margelo::nitro::munimwifi::SuggestionStatus::UNKNOWN: return JSIConverter<std::string>::toJSI(runtime, "unknown");
        case margelo::nitro::munimwifi::SuggestionStatus::UNSUPPORTED: return JSIConverter<std::string>::toJSI(runtime, "unsupported");
        case margelo::nitro::munimwifi::SuggestionStatus::FAILED: return JSIConverter<std::string>::toJSI(runtime, "failed");
        case margelo::nitro::munimwifi::SuggestionStatus::ADDED: return JSIConverter<std::string>::toJSI(runtime, "added");
        case margelo::nitro::munimwifi::SuggestionStatus::ALREADYEXISTS: return JSIConverter<std::string>::toJSI(runtime, "alreadyExists");
        case margelo::nitro::munimwifi::SuggestionStatus::REMOVED: return JSIConverter<std::string>::toJSI(runtime, "removed");
        case margelo::nitro::munimwifi::SuggestionStatus::NOTFOUND: return JSIConverter<std::string>::toJSI(runtime, "notFound");
        case margelo::nitro::munimwifi::SuggestionStatus::ACTIVE: return JSIConverter<std::string>::toJSI(runtime, "active");
        case margelo::nitro::munimwifi::SuggestionStatus::INACTIVE: return JSIConverter<std::string>::toJSI(runtime, "inactive");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert SuggestionStatus to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("unknown"):
        case hashString("unsupported"):
        case hashString("failed"):
        case hashString("added"):
        case hashString("alreadyExists"):
        case hashString("removed"):
        case hashString("notFound"):
        case hashString("active"):
        case hashString("inactive"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
