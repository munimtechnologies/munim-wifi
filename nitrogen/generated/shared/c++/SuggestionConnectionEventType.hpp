///
/// SuggestionConnectionEventType.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::munimwifi {

  /**
   * An enum which can be represented as a JavaScript union (SuggestionConnectionEventType).
   */
  enum class SuggestionConnectionEventType {
    POSTCONNECTION      SWIFT_NAME(postconnection) = 0,
    CONNECTIONFAILURE      SWIFT_NAME(connectionfailure) = 1,
    ERROR      SWIFT_NAME(error) = 2,
  } CLOSED_ENUM;

} // namespace margelo::nitro::munimwifi

namespace margelo::nitro {

  // C++ SuggestionConnectionEventType <> JS SuggestionConnectionEventType (union)
  template <>
  struct JSIConverter<margelo::nitro::munimwifi::SuggestionConnectionEventType> final {
    static inline margelo::nitro::munimwifi::SuggestionConnectionEventType fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("postConnection"): return margelo::nitro::munimwifi::SuggestionConnectionEventType::POSTCONNECTION;
        case hashString("connectionFailure"): return margelo::nitro::munimwifi::SuggestionConnectionEventType::CONNECTIONFAILURE;
        case hashString("error"): return margelo::nitro::munimwifi::SuggestionConnectionEventType::ERROR;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum SuggestionConnectionEventType - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::munimwifi::SuggestionConnectionEventType arg) {
      switch (arg) {
        case margelo::nitro::munimwifi::SuggestionConnectionEventType::POSTCONNECTION: return JSIConverter<std::string>::toJSI(runtime, "postConnection");
        case margelo::nitro::munimwifi::SuggestionConnectionEventType::CONNECTIONFAILURE: return JSIConverter<std::string>::toJSI(runtime, "connectionFailure");
        case margelo::nitro::munimwifi::SuggestionConnectionEventType::ERROR: return JSIConverter<std::string>::toJSI(runtime, "error");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert SuggestionConnectionEventType to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("postConnection"):
        case hashString("connectionFailure"):
        case hashString("error"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
