///
/// NetworkState.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::munimwifi {

  /**
   * An enum which can be represented as a JavaScript union (NetworkState).
   */
  enum class NetworkState {
    UNAVAILABLE      SWIFT_NAME(unavailable) = 0,
    AVAILABLE      SWIFT_NAME(available) = 1,
    LOST      SWIFT_NAME(lost) = 2,
  } CLOSED_ENUM;

} // namespace margelo::nitro::munimwifi

namespace margelo::nitro {

  // C++ NetworkState <> JS NetworkState (union)
  template <>
  struct JSIConverter<margelo::nitro::munimwifi::NetworkState> final {
    static inline margelo::nitro::munimwifi::NetworkState fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("unavailable"): return margelo::nitro::munimwifi::NetworkState::UNAVAILABLE;
        case hashString("available"): return margelo::nitro::munimwifi::NetworkState::AVAILABLE;
        case hashString("lost"): return margelo::nitro::munimwifi::NetworkState::LOST;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum NetworkState - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::munimwifi::NetworkState arg) {
      switch (arg) {
        case margelo::nitro::munimwifi::NetworkState::UNAVAILABLE: return JSIConverter<std::string>::toJSI(runtime, "unavailable");
        case margelo::nitro::munimwifi::NetworkState::AVAILABLE: return JSIConverter<std::string>::toJSI(runtime, "available");
        case margelo::nitro::munimwifi::NetworkState::LOST: return JSIConverter<std::string>::toJSI(runtime, "lost");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert NetworkState to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("unavailable"):
        case hashString("available"):
        case hashString("lost"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
